% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfpca_vd.R
\name{plot.mfpca_vd}
\alias{plot.mfpca_vd}
\title{Plot method for variable domain multivariate FPCA}
\usage{
\method{plot}{mfpca_vd}(
  x,
  type = c("eigenfunctions", "heatmap", "scores"),
  variable = 1,
  components = 1:2,
  domains = NULL,
  align_sign = TRUE,
  ...
)
}
\arguments{
\item{x}{An object of class \code{mfpca_vd}.}

\item{type}{One of \code{"eigenfunctions"} (the default), \code{"heatmap"} or
\code{"scores"}. \code{"eigenfunctions"} draws the chosen components at a
few fixed domains as superimposed lines, \code{"heatmap"} shows one
component across all domains, and \code{"scores"} plots the multivariate
scores colored by domain length.}

\item{variable}{Index of the functional variable to display when
\code{type = "eigenfunctions"} or \code{type = "heatmap"} (default \code{1}).}

\item{components}{Indices of the components to display (default \code{1:2}).
For \code{type = "heatmap"} only the first one is used.}

\item{domains}{Domain lengths at which to draw the eigenfunctions. If
\code{NULL}, a few values spanning the observed domains are used.}

\item{align_sign}{If \code{TRUE} (the default), the sign of each eigenfunction
is aligned across domains so the curves overlay consistently.}

\item{...}{Further graphical arguments.}
}
\value{
Called for its side effect (a plot). Invisibly returns \code{NULL}.
}
\description{
Displays either the estimated eigenfunctions of one variable at several fixed
domain lengths (superimposed lines) or the multivariate scores colored by
domain length.
}
