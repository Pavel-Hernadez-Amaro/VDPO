% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.po_fit}
\alias{plot.po_fit}
\title{Plot method for partially observed functional regression fits}
\usage{
\method{plot}{po_fit}(x, beta_index = 1, ...)
}
\arguments{
\item{x}{an object of class \code{po_fit}, as returned by \code{\link{po_fit}}.}

\item{beta_index}{an integer selecting which functional coefficient to plot.
Default is 1.}

\item{...}{currently ignored, included for compatibility with the generic.}
}
\value{
A \code{ggplot2} object displaying the estimated coefficient and its
confidence band.
}
\description{
Displays an estimated functional coefficient of a \code{po_fit} object
together with its pointwise confidence band.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(42)
  data <- data_generator_po_1d(N = 100)
  grid <- seq(0, 1, length.out = ncol(data$X_se))
  fit <- po_fit(y ~ ffpo(X_se, grid = grid), data = data)
  plot(fit)
}
}

}
\seealso{
\code{\link{po_fit}}
}
